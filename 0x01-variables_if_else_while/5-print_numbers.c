#include <stdio.h>
#include <unistd.h>
/**
 * main- entry point
 *
 * Return: ALways 0 success
 */
int main(void)
{
	int ch;

	for (ch > 0 && ch < 10)
	{
		putchar(ch);
	}

	else
	{
	}
	putchar('\n);
	return (0);
}
