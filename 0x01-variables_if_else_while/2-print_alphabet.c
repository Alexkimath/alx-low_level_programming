/*output alphabets in lowercase*/
#include <stdio.h>
#include <unistd.h>
/**
 * main - entry point
 *
 * Return: Always 0 success
 */
int main(void)
{
	int ap;

	for (ap = 'a'; ap <= 'z'; ap++)

	{
		putchar(ap)
	}
	putchar('\n');
	return (0);
}
